provider awscc {
  region = awscc.Region.ap_northeast_1
}

let env = "Production"

let app = "WebApp"

awscc.ec2.vpc {
  cidr_block = "10.0.0.0/16"

  tags = {
    Name = upper(env)
    App  = lower(app)
  }
}
